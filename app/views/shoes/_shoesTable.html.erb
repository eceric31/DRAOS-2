<table id="shoeTable">
    <% @shoes.find_in_batches(batch_size: 4) do |shoe_batch| %>
        <tr>
        <% shoe_batch.each do |shoe| %>
            <td>
                <div id="shoeData">
                    <%= link_to image_tag(shoe.photo_url, size: "135x135"), shoe_path(shoe), id: "photoImg" %>
                </div>
            </td>
        <% end %>
        <tr>
    <% end %>
</table>

